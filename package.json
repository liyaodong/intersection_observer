{
  "name": "intersetion_observer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server",
    "build": "browserify src/index.js -o build/index.js",
    "build:watch": "watch 'npm run build' ./src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "intersection-observer": "^0.1.1"
  }
}
